// Empty shim for ink's optional react-devtools-core import.
// ink only uses devtools when explicitly enabled; this keeps the bundle
// self-contained for the HERMES_TUI_DIR prebuilt mechanism.
export default {}
